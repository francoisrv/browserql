type State @state {
  counter:      Int !
  name:         String        @default(string: "anonymous") @persist
  story:        String        @default(string: "yeah")
  title:        String !
}

extend type Query {
  
}
