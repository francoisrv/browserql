{
  "name": "@browserql/default",
  "version": "1.1.0",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "graphql": "^14.0.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "jest": "^26.0.1"
  }
}
