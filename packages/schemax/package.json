{
  "name": "@browserql/schemax",
  "version": "1.5.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && yarn tsc",
    "watch": "yarn build -w",
    "test": "jest dist",
    "prepublishOnly": "npm run build && npm test"
  },
  "dependencies": {
    "graphql": "^15.3.0",
    "graphql-tag": "^2.11.0",
    "lodash.find": "^4.6.0",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "@types/lodash.find": "^4.6.6"
  }
}
