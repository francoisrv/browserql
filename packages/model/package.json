{
  "name": "@browserql/model",
  "version": "1.1.0",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepublishOnly": "npm run build && npm t",
    "test": "jest dist"
  },
  "devDependencies": {
    "@types/jest": "^26.0.4",
    "jest": "^26.1.0"
  }
}
